<%= render 'shared/page_title', title: 'Create a New Recipe Style' %>

<%= render 'shared/errors', object: @style %>

<div class="row">
  <div class="well col-md-8 col-md-offset-2">
    <%= form_for @style, class: 'form-horizontal' do |f| %>
      <div class="form-group">
        <div class="col-md-2">
          <%= f.label :name %>
        </div>
        <div class="col-md-10">
          <%= f.text_field :name %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-md-3 col-md-offset-2">
          <%= f.submit "Create Style", class: 'btn btn-success' %>
        </div>
      </div>
    <% end %>
  </div>
</div>
